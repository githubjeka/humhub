modules:
    config:
        WebDriver:
            browser: 'chrome'
            window_size: false # disabled in ChromeDriver
            port: 9515
            capabilities:
                chromeOptions:
                    args: ['--headless']
